services:
  jenkins-script-generator:
    image: jenkins-script-generator
    container_name: jenkins-script-generator-container

    env_file:
      - .env

    ports:
      - "5000:5000"

    restart: always
